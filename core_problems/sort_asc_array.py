# Sort the elements of an array in ascending order 

# Using Python's Built-in sorted() Function, creates new one

def sort_array_ascending(arr):
    sorted_arr = sorted(arr)
    return sorted_arr

# Using the sort() Method (in-place sorting)

def sort_array_ascending_inplace(arr):
    arr.sort()
